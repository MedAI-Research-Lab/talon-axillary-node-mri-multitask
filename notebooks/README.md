# Notebooks

The notebooks are intentionally lightweight and GitHub-renderable. They explain the repository, show the frozen architecture/results, and provide the public-output regeneration command. Full model training is executed through the audited Python runners because long-running stages require checkpoint/resume and output-contract controls.
