# Model checkpoints

Checkpoint binaries are intentionally excluded because of repository size and data-governance constraints. Frozen metric, curve, and component outputs support inspection of the reported results and regeneration of aggregate tables/figures. If checkpoints are distributed later, use a versioned research archive and provide SHA-256 hashes and access URLs here.
