# Model checkpoints

Checkpoint binaries are intentionally excluded because of repository size, governance, and pre-publication distribution constraints. Frozen metric, curve, and component outputs are sufficient to inspect the reported results and regenerate aggregate tables/figures. If checkpoints are released later, publish them through a versioned research archive and add SHA-256 hashes and download URLs here.
