# Data availability

The raw clinical dataset is not distributed. `splits/` contains sanitized, deidentified assignments and audits for the five repeated holdouts. To execute training, place institutionally authorized data under `data/private/` or point a local configuration to another protected location. `data/private/` is excluded by `.gitignore`.
